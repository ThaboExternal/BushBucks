the whole website works now, top to bottom, tested it.

test coverd on the 6th october (could only cover 3 main ones, but the other 3 should work since replica just different coins)

##trade btc file##
buy btc works
sell btc works
buy order works
sell order works

##trade eth file##
buy eth works
sell eth works
buy order works
sell order works

##trade ltc file##
buy ltc works
sell ltc works
buy order works
sell order works

conclusion all buys and sells work for all coins (probable conclusion)

index.php now updates the cryptocurrency table every single time (been tested when creating it)

second round of tests covered on the 6th october (this is testing the stop limit orders now)

## it works, but the calculations are off (the adding to wallet and the subtracting from wallet, fix calculations ##



#### important notice #####
i have commented out some code that uses the api to test the system , and also gone back to not using backbone to test system (6 october)
i will uncomment it out once done testing all features
did this as i have added the update balances when making a stop limit buy, and added status checks to allow the correct things to be used


api keys
D97DE1CA-908D-4512-A65C-DF091720909B	(makes all the buy and sell call data, also makes the wallet update calls)
8E08E111-7899-4FDF-B473-31F639928280	(makes all the calls for graph data)
D4864B54-0B07-4EA1-AD3C-1745B9F23F59	(used to update cryptocurrency price)

echo "<li class=\"nav-item me-3 me-lg-1\">";
        echo "<a class=\"nav-link\" href=\"reports.php?id=" . $_REQUEST['id'] . "\">";
        echo "<span><i class=\"fas fa-file\"></i></span>";
        echo "</a>";
        echo "</li>";

### bootstrap 4 , php , html , css and javascript used in this project ####
#have a backup of all files on github repo (last commit 2 Sep 2021 (02:27 am))
#a lot of the code is repetitve, this is done to take into consideration of not wanting to many stand alone files to complete one task only (i.e like the alter.php does delete and update functions (the delete purges user details from wallet and client table so far, this delete funtion will be extended to delete user records on all tables, and files included)).
# have connected coinapi.io ( need more apikeys (only allowed 100 calls per day))
# got simple trades working for all coins, but havent created a trans table, and definatly going to need a csv to hold reports


--Page layout--
#index.php (main page before login)
#admin.php (user-icon)(once logged in will take you to edit options of profile)(alter.php)
#assist.php (help for user- contactUs) (headset icon)
#trade.php (transactions page) (monera icon)
#wallet.php (where user can view balance and goto trade page)(wallet icon)
#connection.php (users connections with other users)(muli user icon)
#cart.php (where pending trades can be seen and edited)] (so stop limit, buy order and all)
#house button and jet icon will both take you back to the home page
#bell icon, will be for completed trades lookup (so basiclly when a trade from cart is complete, send a notification)
#chat logo for previous chats with support staff (this will only work for logged in users, logged out users wont have a button to do this)
#alter.php for delete and update account details

#might be more added, readme hasnt been updated fully


--used for bootstrapping--
	#col-md-6
	#col-md-3
	#col-sm-6

--tailerd experience per device (phone,desktop,tablet)--
	#@media (max-width: )

--main folder contents--
	#extra folder (where icons and images being used will be found)
	#css folder , style sheets for specifc pages ( some pages use a different style sheet)
	#files folder, will have text files, and , maybe user reports will be added ( then we will added a button link to the file, basic format to each report will be used)
	#not done folder is current php files that need to be created, or in the pipeline but dont need to be in main folder (clutter)
	#tables folder are screenshots of the DB (N.B the database hasnt been fully built, i have only built relavent parts that would have assisted me in the programs i needed to create till date)
	#then completed files / currently being updated/ created or used are just sitting freely (to keep clean)


--CSS--
(will add specifics of which php files actually use these so far)
#style.css
#style1.css
#style2.css
#style3.css



-----Important to do list----
(this is the todo list, add and subtract as needed)

#create database ( tables folder has current database tables, just for testing present completed code)
#can add other forms of navigation, only have global implemented so far, 
#test system in parts , then test whole system when complete (in progress) (current files allwo for users / non users to navigate the website)
#group code files in one folder (completed)
#group css files in one folder (completed)
#group icons/pictures in one folder (completed)
#group legal and etc files in one folder (completed/ in progress need to add text or make them html files still)
#if you look at the code, basic look is carried out throughout the files,just copy and paste base code (basic.txt), then create the page wanted (might have errors, used find all and replace to revert it back to rough draft 1 index.html that i created 28 august 2021)/ but can just look at index.php and copy and paste to new file and edit.
#added way of getting current exchange rates added to DB
#added way of getting current prices (charts of coins)
# have to make 6 trade pages (one for each coin)(researched on luno website)

--- Completed pages -- ( by completed means main function works, but needs maybe ads / text / images added)
#login.php has been tested, check DB for username and password currently in the system
#admin.php has been tested. need to add a log out button
#create.php has been tested and works.
#alter.php has been tested and works (deletes and updates)
#index.php has been tested and works with the parts that have been completed/in progress
#config.php are just the cred to log into DB
#wallet.php has been tested, can be changed to not creating all 6 wallets for users, but definatly will need another php file to handle the button request for creating a wallet per plus button being pressed (client table, only holds the first walletId, wanted to use json array, to store all 6 Ids, but that could happen in the last part of development).
#trade pages, but need to add the buy limit and the stop limit, thats one thats going to be tricky
